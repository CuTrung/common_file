ARG MONGO_VERSION=latest
FROM mongo:${MONGO_VERSION}

ARG SERVICE_NAME=mongo
ENV SERVICE_NAME $SERVICE_NAME

ARG HOST=localhost
ENV HOST $HOST

ARG REPLICATE_NAME=replicate
ENV REPLICATE_NAME $REPLICATE_NAME

CMD mongod --replSet ${REPLICATE_NAME} --bind_ip ${HOST},${SERVICE_NAME}